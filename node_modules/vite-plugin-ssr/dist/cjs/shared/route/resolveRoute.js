"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveRoute = void 0;
const utils_1 = require("./utils");
const resolveRouteString_1 = require("./resolveRouteString");
function resolveRoute(routeString, urlPathname) {
    const errMsg = (propName, msg = 'a non-empty string') => `[resolveRoute(routeString, urlPathname)] \`${propName}\` should be ` + msg;
    (0, utils_1.assertUsage)(routeString, errMsg('routeString'));
    (0, utils_1.assertUsage)(urlPathname, errMsg('urlPathname'));
    (0, utils_1.assertUsage)(urlPathname.startsWith('/'), errMsg('urlPathname', '`pageContext.urlPathname`'));
    const result = (0, resolveRouteString_1.resolveRouteString)(routeString, urlPathname);
    return {
        match: !!result,
        routeParams: result?.routeParams ?? {}
    };
}
exports.resolveRoute = resolveRoute;
