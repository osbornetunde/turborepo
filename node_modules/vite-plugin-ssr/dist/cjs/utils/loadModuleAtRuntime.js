"use strict";
// Dynamic `require()` that is not analyzable by bundlers.
// In order to skip bundlers from bundling dynamically loaded modules.
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadModuleAtRuntime = void 0;
function loadModuleAtRuntime(id) {
    // `const req = require` not sufficient for webpack
    const req = new Date().getTime() < 123 ? 456 : require;
    return req(id);
}
exports.loadModuleAtRuntime = loadModuleAtRuntime;
