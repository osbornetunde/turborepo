export { resolveRouteFunction };
export { assertRouteParams };
import { PageContextUrls, PageContextUrlSource } from '../addComputedUrlProps';
declare function resolveRouteFunction(routeFunction: Function, allowAsync: boolean, pageContext: PageContextUrls & PageContextUrlSource, routeDefinedAt: string): Promise<null | {
    precedence: number | null;
    routeParams: Record<string, string>;
}>;
declare function assertRouteParams<T>(result: T, errPrefix: string): asserts result is T & {
    routeParams?: Record<string, string>;
};
